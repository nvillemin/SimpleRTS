using UnityEngine;
using System.Collections;

namespace RTS {
	// All different cursor states
	public enum CursorState { Select, Move, Attack, PanLeft, PanRight, PanUp, PanDown, Harvest }

	// All resource types
	public enum ResourceType { Money, Power }
}
